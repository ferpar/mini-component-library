import { Meta, Story } from "@storybook/addon-docs/blocks";
import ProgressBar from "./ProgressBar";

<Meta
  title="ProgressBar"
  component={ProgressBar}
  argTypes={{
    value: {
      control: {
        type: "number",
        min: 0,
        max: 100,
      },
    },
    size: {
      control: {
        type: "select",
        options: ["small", "medium", "large"],
      },
    },
    value: {
      control: {
        type: "number",
        min: 20,
        max: 3000,
      },
    },
  }}
/>

export const Template = (args) => <ProgressBar {...args} />;

# ProgressBar

<Story name="Default" args={{ value: 50, size: "medium", width: 372 }}>
  {Template.bind({})}
</Story>
